# Railway Config as Code
# Deploys the complete ibor-analyst stack to Railway
# Docs: https://docs.railway.app/config-as-code

[build]
builder = "RAILPACK"
# Each service has its own railway.toml in its directory

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
