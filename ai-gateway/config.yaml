ibor:
  api_base: http://localhost:8080/api
  verify_ssl: true

anthropic:
  model: claude-sonnet-4-6

openai:
  embedding_model: text-embedding-3-small  # still used for RAG embeddings

database:
  dsn: postgresql://ibor:ibor@localhost:5432/ibor

# Secrets (OPENAI_API_KEY, PG_DSN override) must be set via environment variables or .env file.
# Never add secret values here.
