server:
  port: 8080
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/ibor}
    username: ${DB_USER:ibor}
    password: ${DB_PASSWORD:ibor}
  jpa:
    hibernate:
      ddl-auto: none   # keep schema from SQL; or "validate"
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
      hibernate.jdbc.time_zone: UTC
    open-in-view: false
openai:
  api-key: ${OPENAI_API_KEY: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}
  baseUrl: ${OPENAI_BASE_URL:https://api.openai.com/v1}
  chatModel: ${OPENAI_CHAT_MODEL:gpt-4o-mini}
  embedModel: ${OPENAI_EMBED_MODEL:text-embedding-3-small}
rag:
  topK: 6
  chunkChars: 1000
  chunkOverlap: 150

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:s:
    health:
      show-details: always
  health:
    db:
      enabled: true
